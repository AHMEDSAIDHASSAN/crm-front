# Copy to `.env.development.local` (gitignored) when the remote dev DB is unreachable.
# Repo root: `docker compose up -d` then from backend: `npx prisma migrate deploy` (or `db:push` if you prefer).
DATABASE_URL="mysql://root:realstate_local@127.0.0.1:3306/crm"
